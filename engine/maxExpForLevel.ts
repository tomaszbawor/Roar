/**
 * Function returns EXP needed for level up from provided level.
 * @param level Level that you are currently on
 */
export const maxExpForLevel = (level: number) => {
  // TODO: Implement
  return level * 500;
};
